   # for n, w in enumerate(eval_w):
    #     id = (rewards * w).sum(axis=1)
    #     print(n, w, id)
    
    # w_front = calc_opt_reward(eval_w, rewards)
    # df = pd.DataFrame(w_front)
    # df.to_csv('test.csv')